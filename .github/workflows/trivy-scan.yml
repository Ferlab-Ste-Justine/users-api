name: Security Scan with Trivy

on:
  pull_request:
    branches:
      - main

permissions:
  contents: read
  security-events: read

jobs:
  scan-dependencies:
    name: Scan Dependencies
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v3
      - name: Run Trivy FS scan (Dependencies)
        uses: aquasecurity/trivy-action@ed142fd0673e97e23eac54620cfb913e5ce36c25 # v0.36.0
        with:
          scan-type: 'fs'
          scan-ref: '.'
          severity: 'HIGH,CRITICAL'
          # Report-only for now: pre-existing dependency advisories, none introduced by this branch.
          # Restore to '1' with the dependency remediation.
          exit-code: '0'

  scan-docker:
    name: Scan Docker Image
    runs-on: ubuntu-latest
    needs: scan-dependencies 
    steps:
      - name: Checkout code
        uses: actions/checkout@v3
      - name: Build Docker Image
        run: docker build -t my-app:${{ github.sha }} .
      - name: Run Trivy Image scan (Docker)
        uses: aquasecurity/trivy-action@ed142fd0673e97e23eac54620cfb913e5ce36c25 # v0.36.0
        with:
          scan-type: 'image'
          image-ref: 'my-app:${{ github.sha }}'
          format: 'table'
          severity: 'HIGH,CRITICAL'
          # Report-only for now: pre-existing dependency advisories, none introduced by this branch.
          # Restore to '1' with the dependency remediation.
          exit-code: '0'